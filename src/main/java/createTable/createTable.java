package createTable;

public class createTable {

/*

 create table turkey_create_lecture
(
subjectname varchar2(128),
classtime varchar2(128),
lectureroom varchar2(128),
department varchar2(128),
professor varchar2(128),
lectureNo number primary key,
subjectnumber VARCHAR2(128)
);

create table turkey_lecture_info
(
    indexId             number primary key,
    department          varchar2(128),
    subjectNumber       varchar2(128),
    subjectName         varchar2(128),
    classTime           varchar2(128),
    LectureRoom         varchar2(128),
    professor           varchar2(128),
    lecture_credit      number,
    subscription_limit  number,
    currentstudent number
);

create table turkey_login_prof
(
    pf_id number
);

create table turkey_login_user
(
	    user_id varchar2(128),
	    user_name varchar2(128)
);

create table turkey_admin
(
    admin_id varchar2(128),
admin_pw varchar2(128)
);

create table turkey_prof
(
pf_name varchar2(128),
pf_dept varchar2(128),
pf_no number primary key
);

create table turkey_user
(
sd_id varchar2(128) primary key,
sd_pw varchar2(128),
sd_name varchar2(128),
sd_email varchar2(128),
sd_pn varchar2(128),
sd_credit number default 18
);

create table turkey_user_dept
(
	    user_id varchar2(128),
	    dept_no number,
	    dept_id number,
        constraint turkey_user_dept_pk primary key(user_id, dept_no)
);


*/
	
	
}
